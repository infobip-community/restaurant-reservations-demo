export interface Field {
    name: string;
    value: string;
}

export enum FIELD_NAME {
    hour = 'hour',
    date = 'date',
    hostName = 'host name',
    hostEmail = 'host email',
    phoneNumber = 'host phone number'
}
